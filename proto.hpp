#include <iostream>
#include <vector>
#ifndef PROTO_H    
#define PROTO_H     
#pragma once

template <typename T>

class vector131{
    private:
        int size_Array;
        T *vec

        void push_back;
        void push_front;
        int resize;

        void print;
    public:
        vector131() {size; vector = }

};

#endif

